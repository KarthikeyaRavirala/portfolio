import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Settings for local development (GitHub Pages settings commented out)
  // output: "export",
  // basePath: "/portfolio",
  // assetPrefix: "/portfolio/",
};

export default nextConfig;